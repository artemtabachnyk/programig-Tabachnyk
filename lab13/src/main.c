/**
 * @file main.c
 * @brief Головний файл програми.
 */

#include "lib.h"

/**
 * @brief Головна функція програми.
 *
 * Функція `main` є головною функцією програми. Викликає функцію `text` для обробки тексту та знаходження найдовшого та найкоротшого слова.
 *
 * @return 0 у разі успішного завершення програми.
 */
int main(){
    text();
    return 0;
}
